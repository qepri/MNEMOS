# Dependencies are managed with pip-tools.
#
#   edit          -> requirements.in        (unpinned, human-authored)
#   regenerate    -> requirements.lock.txt  (fully pinned, used by the build)
#
# Regenerate from inside the build image so resolved wheels match runtime:
#   docker run --rm -v "${PWD}:/w" -w //w python:3.11-slim #     sh -c "pip install pip-tools && pip-compile --strip-extras #            --output-file=requirements.lock.txt requirements.in"
#
# The Dockerfile installs requirements.lock.txt. This file is kept only so
# existing tooling that expects requirements.txt finds this note.
-r requirements.lock.txt
