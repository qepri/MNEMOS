[pytest]
testpaths = tests
markers =
    api: Flask API route tests
    service: backend service-layer tests
    pipeline: Celery pipeline stage tests
    migration: Alembic migration chain tests (own scratch database)
    slow: deselectable during fast local iteration
addopts = --strict-markers -m "not migration"
